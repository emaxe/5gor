# OPT-05 — Кеш высот ландшафта heightAt()

**Приоритет:** 🟢 Лёгкое (эффект переоценён в первой редакции — см. "Честная оценка")
**Сложность:** Минимальная
**Файлы:** `src/citygen.js`
**Ожидаемый прирост:** <0.1 мс/кадр — заметно меньше, чем изначально заявленные «+2-3% FPS»

---

## Описание проблемы

`heightAt(x, z)` вызывается каждый кадр для каждого пешехода и машины игрока:

```js
// peds.js:536 — 1 вызов на пешехода за кадр
const h = this.world ? this.world.heightAt(wp.x, wp.z) : 0;

// citygen.js:65 — содержит перебор _strips (серпантин)
heightAt(x, z) {
  let h = 0;
  // ..вычисления горы Машук..
  for (const s of this._strips) {  // ← до 10 итераций (10 сегментов серпантина)
    if (x >= s.x0 && x <= s.x1 && z >= s.z0 && z <= s.z1) { ... }
  }
  return h;
}
```

Для большинства пешеходов (они ходят по плоскому городу) `heightAt()` всегда возвращает 0 — но
вычисления всё равно выполняются.

### Честная оценка масштаба

Пешеходов в игре ровно 30 (`peds.js:138`, `for (let i = 0; i < 30; i++)`), `heightAt()` вызывается
по разу за кадр на каждого (`peds.js:536`, внутри `_sync()`). Итого: 30 × 60 fps = 1800 вызовов/с,
× до 10 итераций по `_strips` ≈ 18 000 операций сравнения в секунду. Это заметно меньше 0.1 мс
на кадр — заявленные в первой редакции «+2-3% FPS» завышены на порядок. Правку стоит делать
(она бесплатна и корректна), но не рассчитывать, что она заметно изменит FPS в замере.

---

## Решение: bbox early-exit

```js
// citygen.js:65 — добавить в начало heightAt():
heightAt(x, z) {
  // Гора Машук центрирована на (0, -440), радиус 170м (this.hill, citygen.js:33)
  // Серпантин: x ∈ [6.5, 73.5], z ∈ [-440, -256] (segs, citygen.js:326-335)
  // Вторая вершина: центр (55, -500), радиус 70
  // Если точка вне bbox горы и серпантина — гарантированно h=0
  if (Math.abs(x) > 200 || z > -230) return 0; // плоский город — основная зона, где ходят пешеходы

  // ... остальной расчёт без изменений (гора + серпантин)
}
```

Это устраняет вычисления горы и перебор `_strips` для точек в городской зоне за одну проверку —
именно там ходит абсолютное большинство пешеходов.

⚠️ Убран вариант B из первой редакции (LUT 200×200 с ячейкой 4 м). Причины:
- при заявленном масштабе (~18k операций/с) LUT экономит <0.1 мс/кадр — 160 КБ памяти и время
  предрасчёта на `build()` не оправданы;
- lookup по ближайшей ячейке 4 м на серпантине шириной 7.2 м (`citygen.js:349` — `BoxGeometry(7.2, ...)`)
  даёт ступенчатую высоту вместо гладкой интерполяции по `_strips` — визуальная регрессия
  под колёсами игрока при подъёме (`player.snapToTerrain`, `game.js:661`), причём ради выигрыша,
  который не измерим на глаз.

---

## Чеклист

- [ ] Добавить bbox-проверку в начало `heightAt()`: если точка вне зоны горы/серпантина → return 0
- [ ] Использовать bbox `x ∈ [-200, 200], z > -230` (соответствует `this.hill` и `segs` в текущем коде — не расширять без пересчёта границ серпантина)
- [ ] Протестировать: пешеходы в городе корректно стоят на высоте 0
- [ ] Протестировать: машина игрока корректно взбирается на серпантин (bbox не отрезает часть горной зоны)
- [ ] Замерить `peds.update()` время до и после в DevTools Performance — ожидать изменение в пределах шума измерения, не заметный прирост FPS
- [ ] После правки выполнить `python3 build.py` — иначе изменения не попадут в `index.html`
